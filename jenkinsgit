pipeline{
	agent any
	stages{
		stage("welcome"){
			steps{
				git credentialsId: 'Githubid', url: 'https://github.com/skumarhr100/sampletest'
			}
		}
		stage("Maven Build"){
			steps{
				sh "mvn clean"
			}
		
		}
	
	}
}
