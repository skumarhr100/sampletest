pipeline{
	agent any
	tools{
		maven 'Maven'
	}
	stages{
		stage("welcome"){
			steps{
				echo "starting welcome process"
			}
		}
		stage("Maven Build"){
			steps{
				echo "Starting test process"
				
			}
		
		}
	
	}
}
